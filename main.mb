struct Person
{
    name: String,
    surname: String,
    age: int,
}

enum TokenType
{
    Int,
    Float,
}

fn main() -> int {
    let x: int = 1;
    let y: int = 2;

    let mem = Mem::allocate();
    defer mem.deallocate();

    return 0;
}

fn add(x: int*, y: int*) -> int {
    return *x + *y;
}

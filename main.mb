pub struct Person
{
   pub age: int
}

enum NodeType 
{
    Function,
    Enum,
    Struct,
    Impl
}

impl Person {

    fn (p:Person*) set_age(age:int) -> void
    {
        p->age = age;
    }

    fn name() -> str 
    {
        return "";
    }
        
}

fn add(x:int,y:int) -> int {
    return x + y;
}

fn main () -> int {

    x[1] = 1;

    return 0;
}
